<%= notice %>
<h1>Books</h1>

<% @lists.each do |list| %>

<p>Title</p>
<span><%= list.title %></span>

<p> Body</p>
<span><%= list.body %></span>

<p>
<span><%= link_to "show", list_path(list.id) %></span>
</p>

<p>
<span><%= link_to "edit", edit_list_path(list.id) %></span>
</p>

<span>
<%= link_to "destroy", list_path(list.id), method: :delete, "data-confirm"=>"Are you sure?" %>
</span>

<% end %>

<%= form_with model: @list,yrl:'/lists', method: :post do |f| %>
 <h4>Title</h4>
 <%= f.text_field :title %>
 
 <h4>Body</h4>
 <%= f.text_area :body %>
 
 <%= f.submit 'Create Book' %>
<% end %>